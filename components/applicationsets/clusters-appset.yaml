---
apiVersion: argoproj.io/v1beta1
kind: ApplicationSet
metadata:
  name: acm-clusters
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: https://github.com/nocturnalastro/vse-carslab-hub
        revision: deploy_venom
        directories:
          - path: clusters/overlays/cpe03
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 15
          backoff:
            duration: 15s
            factor: 2
            maxDuration: 5m
      source:
        repoURL: https://github.com/nocturnalastro/vse-carslab-hub
        targetRevision: deploy_cpe03
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
