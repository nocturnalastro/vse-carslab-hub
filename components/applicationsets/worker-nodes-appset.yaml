---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: add-day2-workers
  namespace: openshift-gitops
spec:
  generators:
    - git:
        repoURL: https://github.com/nocturnalastro/vse-carslab-hub
        revision: deploy_volante_and_pipeline
        directories:
          - path: add-workers/du3-ldc1
          - path: add-workers/du4-ldc1
  template:
    metadata:
      name: '{{path.basename}}'
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        retry:
          limit: 15
          backoff:
            duration: 15s
            factor: 2
            maxDuration: 5m
      source:
        repoURL: https://github.com/nocturnalastro/vse-carslab-hub
        targetRevision: deploy_volante_and_pipeline
        path: '{{path}}'
      destination:
        server: https://kubernetes.default.svc
